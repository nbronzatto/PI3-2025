{
    "name": "meu-projeto",
    "version": "1.0.0",
    "scripts": {
       "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix interface\""
    },
    "devDependencies": {
      "concurrently": "^8.0.0"
    }
  }
  